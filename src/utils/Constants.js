export const TILE_SIZE = 32;
export const ROOM_WIDTH = Math.floor(window.innerWidth / TILE_SIZE);
export const ROOM_HEIGHT = Math.floor(window.innerHeight / TILE_SIZE);
export const MAP_WIDTH = ROOM_WIDTH * 5;
export const MAP_HEIGHT = ROOM_HEIGHT * 5;